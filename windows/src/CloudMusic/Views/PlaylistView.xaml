<UserControl x:Class="CloudMusic.Views.PlaylistView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Image Source="{Binding CurrentPlaylist.CoverImgUrl}" Width="110" Height="110" Stretch="UniformToFill" />
            <StackPanel Grid.Column="1" Margin="14,0,0,0" VerticalAlignment="Center">
                <TextBlock Text="{Binding CurrentPlaylist.Name}" FontSize="20" FontWeight="Bold" TextWrapping="Wrap" />
                <TextBlock Foreground="{StaticResource SubTextBrush}" Margin="0,6,0,0">
                    <Run Text="创建者：" /><Run Text="{Binding CurrentPlaylist.Creator.Nickname}" />
                </TextBlock>
                <TextBlock Foreground="{StaticResource SubTextBrush}">
                    <Run Text="播放次数：" /><Run Text="{Binding CurrentPlaylist.PlayCount}" />
                    <Run Text="   歌曲数：" /><Run Text="{Binding CurrentPlaylist.TrackCount}" />
                </TextBlock>
                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                    <Button Content="播放全部" Style="{StaticResource PrimaryButton}" Click="PlayAll_Click" />
                    <Button x:Name="SubscribeBtn" Margin="12,0,0,0" Click="Subscribe_Click">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                <Setter Property="Content" Value="收藏歌单" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CurrentPlaylist.Subscribed}" Value="True">
                                        <Setter Property="Content" Value="取消收藏" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </StackPanel>
        </Grid>

        <ListBox x:Name="SongList" Grid.Row="1" Margin="0,12,0,0"
                 ItemsSource="{Binding PlaylistSongs}"
                 MouseDoubleClick="SongList_MouseDoubleClick"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="4,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="36" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding Index, RelativeSource={RelativeSource AncestorType=ListBoxItem}}"
                                   Foreground="{StaticResource SubTextBrush}" VerticalAlignment="Center" />
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding ArtistNames}" FontSize="12" Foreground="{StaticResource SubTextBrush}" />
                        </StackPanel>
                        <TextBlock Grid.Column="2" Text="{Binding DurationText}" FontSize="12"
                                   Foreground="{StaticResource SubTextBrush}" VerticalAlignment="Center" />
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
