<UserControl x:Class="GlassSuite.Views.GitHubView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="GitHub 检索" FontSize="20" FontWeight="Bold" />
            <TextBlock Text="搜索仓库 / 热门趋势 · 双击打开" FontSize="12" Foreground="{StaticResource SubTextBrush}" Margin="0,2,0,10" />
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="QueryBox" Height="32" FontSize="13" VerticalContentAlignment="Center"
                     Text="{Binding GitHubQuery, UpdateSourceTrigger=PropertyChanged}" />
            <Button Grid.Column="1" Content="搜索" Style="{StaticResource PrimaryButton}" Margin="10,0,0,0" Click="Search_Click" />
        </Grid>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock x:Name="StatusText" FontSize="12" Foreground="{StaticResource SubTextBrush}" Margin="0,0,0,6" />
            <ListBox x:Name="RepoList" Grid.Row="1" MouseDoubleClick="RepoList_MouseDoubleClick"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="4,6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding FullName}" FontSize="14" FontWeight="SemiBold" />
                                    <TextBlock Text="  ★ " Foreground="#F5A623" FontSize="12" VerticalAlignment="Center" />
                                    <TextBlock Text="{Binding StargazersCount}" Foreground="#F5A623" FontSize="12" VerticalAlignment="Center" />
                                </StackPanel>
                                <TextBlock Text="{Binding Description}" FontSize="12" Foreground="{StaticResource SubTextBrush}"
                                           TextWrapping="Wrap" MaxWidth="560" Margin="0,2,0,0" />
                                <TextBlock Text="{Binding Language}" FontSize="11" Foreground="#3D7FFF" Margin="0,2,0,0" />
                            </StackPanel>
                            <TextBlock Grid.Column="1" Text="打开 ↗" Foreground="#3D7FFF" FontSize="12" VerticalAlignment="Center" />
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Grid>
</UserControl>
