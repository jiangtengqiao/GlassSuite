<UserControl x:Class="GlassSuite.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="璃光" FontSize="34" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" />
            <TextBlock Text="GlassSuite · 登录网易云账号" FontSize="14" Foreground="{StaticResource SubTextBrush}" Margin="0,8,0,24" />
            <TabControl x:Name="LoginTabs" Width="340" SelectedIndex="0">
                <TabItem Header="扫码登录">
                    <StackPanel Margin="0,16">
                        <Image x:Name="QrImage" Width="210" Height="210" Stretch="Uniform" />
                        <TextBlock x:Name="QrStatus" Text="正在获取二维码…" Foreground="{StaticResource SubTextBrush}"
                                   HorizontalAlignment="Center" Margin="0,12,0,0" />
                        <TextBlock Text="1. 打开网易云音乐 App  2. 右上角「我的」扫码  3. 扫描并确认"
                                   Foreground="#BBBBBB" FontSize="12" HorizontalAlignment="Center" Margin="0,8,0,0" />
                    </StackPanel>
                </TabItem>
                <TabItem Header="验证码登录">
                    <StackPanel Margin="0,16">
                        <TextBox x:Name="PhoneBox" MaxLength="11" Height="34" FontSize="14"
                                 ToolTip="手机号" />
                        <Grid Margin="0,10,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="CodeBox" MaxLength="6" Height="34" FontSize="14" ToolTip="验证码" />
                            <Button x:Name="SendBtn" Grid.Column="1" Content="发送验证码" Margin="10,0,0,0" Click="SendBtn_Click" />
                        </Grid>
                        <Button Content="登 录" Style="{StaticResource PrimaryButton}" FontSize="16" Height="40" Margin="0,16,0,0"
                                Click="LoginBtn_Click" />
                        <TextBlock x:Name="SmsStatus" Foreground="#E84026" FontSize="13" Margin="0,8,0,0" />
                    </StackPanel>
                </TabItem>
            </TabControl>
        </StackPanel>

        <Grid Grid.Column="1" Background="#F7F7F9" />
    </Grid>
</UserControl>
