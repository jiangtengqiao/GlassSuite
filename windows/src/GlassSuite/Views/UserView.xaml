<UserControl x:Class="GlassSuite.Views.UserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Ellipse Width="64" Height="64">
                <Ellipse.Fill>
                    <ImageBrush ImageSource="{Binding User.AvatarUrl}" Stretch="UniformToFill" />
                </Ellipse.Fill>
            </Ellipse>
            <StackPanel Grid.Column="1" Margin="14,0,0,0" VerticalAlignment="Center">
                <TextBlock Text="{Binding User.Nickname}" FontSize="20" FontWeight="Bold" />
                <TextBlock Text="{Binding User.Signature}" FontSize="12" Foreground="{StaticResource SubTextBrush}"
                           TextWrapping="Wrap" Margin="0,4,0,0" />
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,14,0,0">
            <Button Content="我喜欢" Style="{StaticResource PrimaryButton}" Click="Liked_Click" />
            <Button Content="设置" Margin="12,0,0,0" Click="Settings_Click" />
            <Button Content="退出登录" Margin="12,0,0,0" Foreground="#E84026" Click="Logout_Click" />
        </StackPanel>

        <TextBlock Grid.Row="2" Text="我的歌单" FontSize="16" FontWeight="Bold" Margin="0,18,0,8" />

        <TabControl Grid.Row="3">
            <TabItem Header="歌单">
                <ListBox x:Name="UserPlaylistList" ItemsSource="{Binding UserPlaylists}"
                         MouseDoubleClick="Playlist_MouseDoubleClick"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="4,3">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Image Source="{Binding CoverImgUrl}" Width="44" Height="44" Stretch="UniformToFill" />
                                <StackPanel Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold" />
                                    <TextBlock Text="{Binding TrackCount}" FontSize="12" Foreground="{StaticResource SubTextBrush}" />
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </TabItem>
            <TabItem Header="我喜欢">
                <ListBox x:Name="LikedSongList" ItemsSource="{Binding LikedSongs}"
                         MouseDoubleClick="LikedSongList_MouseDoubleClick"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="4,3">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold" />
                                    <TextBlock Text="{Binding ArtistNames}" FontSize="12" Foreground="{StaticResource SubTextBrush}" />
                                </StackPanel>
                                <TextBlock Grid.Column="1" Text="{Binding DurationText}" FontSize="12"
                                           Foreground="{StaticResource SubTextBrush}" VerticalAlignment="Center" />
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
