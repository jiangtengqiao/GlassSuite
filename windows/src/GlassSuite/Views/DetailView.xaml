<UserControl x:Class="GlassSuite.Views.DetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- ============ 歌曲详情面板 ============ -->
        <Grid Grid.Row="0" Visibility="Collapsed">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Visibility" Value="Visible" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding DetailSong}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Image Source="{Binding DetailSong.AlbumPic}" Width="120" Height="120" Stretch="UniformToFill" />
            <StackPanel Grid.Column="1" Margin="14,0,0,0" VerticalAlignment="Center">
                <TextBlock Text="{Binding DetailSong.Name}" FontSize="20" FontWeight="Bold" TextWrapping="Wrap" />
                <TextBlock Foreground="{StaticResource SubTextBrush}" Margin="0,6,0,0">
                    <Run Text="歌手：" /><Run Text="{Binding DetailSong.ArtistNames}" />
                </TextBlock>
                <TextBlock Foreground="{StaticResource SubTextBrush}">
                    <Run Text="专辑：" /><Run Text="{Binding DetailSong.AlbumName}" />
                    <Run Text="   时长：" /><Run Text="{Binding DetailSong.DurationText}" />
                </TextBlock>
                <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                    <Button Content="播放" Style="{StaticResource PrimaryButton}" Click="Play_Click" />
                    <Button x:Name="LikeBtn" Margin="12,0,0,0" Click="Like_Click">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                <Setter Property="Content" Value="♡ 喜欢" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsLiked}" Value="True">
                                        <Setter Property="Content" Value="♥ 已喜欢" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    <Button Content="查看歌手" Margin="12,0,0,0" Click="Artist_Click" />
                    <Button Content="查看专辑" Margin="12,0,0,0" Click="Album_Click" />
                    <Button Content="观看 MV" Margin="12,0,0,0" Click="Mv_Click" />
                </StackPanel>
            </StackPanel>
        </Grid>

        <!-- ============ 歌手详情面板 ============ -->
        <Grid Grid.Row="0" Grid.RowSpan="2" Visibility="Collapsed">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Visibility" Value="Visible" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding DetailArtist}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <Image Source="{Binding DetailArtist.PicUrl}" Width="72" Height="72" Stretch="UniformToFill" />
                <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="{Binding DetailArtist.Name}" FontSize="20" FontWeight="Bold" />
                    <Button Content="播放热门歌曲" Style="{StaticResource PrimaryButton}" Margin="0,8,0,0" Click="ArtistPlay_Click" />
                </StackPanel>
            </StackPanel>
            <TextBlock Grid.Row="1" Text="热门歌曲" FontSize="16" FontWeight="Bold" Margin="0,4,0,4" />
            <ListBox x:Name="ArtistSongList" Grid.Row="2" ItemsSource="{Binding DetailArtistSongs}"
                     MouseDoubleClick="ArtistSongList_MouseDoubleClick">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="36" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding Index, RelativeSource={RelativeSource AncestorType=ListBoxItem}}"
                                       Foreground="{StaticResource SubTextBrush}" VerticalAlignment="Center" />
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{Binding Name}" FontSize="14" />
                                <TextBlock Text="{Binding AlbumName}" FontSize="12" Foreground="{StaticResource SubTextBrush}" />
                            </StackPanel>
                            <TextBlock Grid.Column="2" Text="{Binding DurationText}" FontSize="12"
                                       Foreground="{StaticResource SubTextBrush}" VerticalAlignment="Center" />
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <TextBlock Grid.Row="2" Text="专辑" FontSize="16" FontWeight="Bold" Margin="0,10,0,4"
                       VerticalAlignment="Top" Visibility="Collapsed" />
            <ListBox x:Name="AlbumList" Grid.Row="2" ItemsSource="{Binding DetailAlbums}" Margin="0,40,0,0"
                     MouseDoubleClick="ArtistAlbum_MouseDoubleClick">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="2">
                            <Image Source="{Binding PicUrl}" Width="44" Height="44" Stretch="UniformToFill" />
                            <StackPanel Margin="8,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Name}" FontSize="13" />
                                <TextBlock Text="{Binding Size}" FontSize="11" Foreground="{StaticResource SubTextBrush}" />
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>

        <!-- ============ 专辑详情面板 ============ -->
        <Grid Grid.Row="0" Grid.RowSpan="2" Visibility="Collapsed">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Visibility" Value="Visible" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding DetailAlbum}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <Image Source="{Binding DetailAlbum.PicUrl}" Width="84" Height="84" Stretch="UniformToFill" />
                <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="{Binding DetailAlbum.Name}" FontSize="18" FontWeight="Bold" TextWrapping="Wrap" />
                    <TextBlock Text="{Binding DetailAlbum.Artist.Name}" Foreground="{StaticResource SubTextBrush}" Margin="0,4,0,0" />
                    <Button Content="播放全部" Style="{StaticResource PrimaryButton}" Margin="0,8,0,0" Click="AlbumPlay_Click" />
                </StackPanel>
            </StackPanel>
            <ListBox x:Name="AlbumSongList" Grid.Row="1" ItemsSource="{Binding DetailAlbumSongs}"
                     MouseDoubleClick="AlbumSongList_MouseDoubleClick">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="36" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding Index, RelativeSource={RelativeSource AncestorType=ListBoxItem}}"
                                       Foreground="{StaticResource SubTextBrush}" VerticalAlignment="Center" />
                            <TextBlock Grid.Column="1" Text="{Binding Name}" FontSize="14" VerticalAlignment="Center" />
                            <TextBlock Grid.Column="2" Text="{Binding DurationText}" FontSize="12"
                                       Foreground="{StaticResource SubTextBrush}" VerticalAlignment="Center" />
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Grid>
</UserControl>
